package org.sbolstandard.converter.sbol23_31.measurement;

import java.net.URI;

import org.sbolstandard.converter.Util;
import org.sbolstandard.converter.sbol23_31.EntityConverter;
import org.sbolstandard.converter.sbol23_31.Parameters;
import org.sbolstandard.core3.entity.Collection;
import org.sbolstandard.core3.entity.SBOLDocument;
import org.sbolstandard.core2.SBOLValidationException;
import org.sbolstandard.core3.util.SBOLGraphException;

public class UnitConverter {//implements EntityConverter<Unit, Unit>  {

    // @Override
    // public Unit convert(SBOLDocument doc, org.sbolstandard.core2. input, Parameters parameters) throws SBOLGraphException {  
    //     Unit unit = doc.createPre(Util.createSBOL3Uri(input),Util.getNamespace(input));
    //     Util.copyIdentified(input, unit, parameters);
    //     for (URI memberUri : input.getMemberURIs()) {
    //     	col.addMember(Util.createSBOL3Uri(memberUri, parameters) );
    //     }
    //     return col;
    // }
}
